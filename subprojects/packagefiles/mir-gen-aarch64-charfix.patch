diff --git a/mir-gen-aarch64.c b/mir-gen-aarch64.c
index 4dc6c08..8ab0128 100644
--- a/mir-gen-aarch64.c
+++ b/mir-gen-aarch64.c
@@ -2168,7 +2168,14 @@ static void out_insn (gen_ctx_t gen_ctx, MIR_insn_t insn, const char *replacemen
       && (insn->ops[1].mode == MIR_OP_INT || insn->ops[1].mode == MIR_OP_UINT))
     insn->ops[1].u.u = (insn->ops[1].u.u + 15) & -16;
   for (insn_str = replacement;; insn_str = p + 1) {
-    char ch, ch2, start_ch, d;
+    char ch, ch2, start_ch;
+    /* `d` holds hex_value()'s result (-1 for non-hex) and is tested `>= 0` as a
+       loop terminator. It MUST be int: on targets where `char` is unsigned by
+       default (e.g. AArch64 Linux per AAPCS64; Apple arm64 keeps char signed),
+       a `char d` truncates -1 to 255, so `d >= 0` never fails and the template
+       hex-parse loop runs off the instruction string into unmapped memory
+       (segfault at -O0, spin/hang at -O2). read_hex() above already uses int. */
+    int d;
     uint32_t opcode = 0, opcode_mask = 0xffffffff;
     int rd = -1, rn = -1, rm = -1, ra = -1, disp = -1, scale = -1;
     int immr = -1, imms = -1, imm16 = -1, imm16_shift = -1, imm12 = -1, imm12_shift = -1;
