# src/rocs/meson.build — rocs source aggregation.
#
# WP-001 Phase 1 folds the rocs module into libn00b directly. Later
# WP-001 phases extend this source list with mapped-image implementation
# files while keeping the root build aggregation stable.

n00b_rocs_src = files(
    'filter.c',
    'index.c',
    'map.c',
    'normalizer.c',
    'plan.c',
    'shard.c',
    'store.c',
)
