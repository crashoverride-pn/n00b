# Cross-cutting utilities used by the core.

subdir('simd')

n00b_util_src = (
    files(
        'assert.c',
        'dynamic_lib.c',
        'errno_str.c',
        'marshal.c',
        'parse_num.c',
        'path.c',
        'proc.c',
        'queue.c',
        'worker_pool.c',
    )
    + n00b_util_simd_src
)
